import os
import random
import hashlib
from datetime import datetime

print(datetime.now())

'''
print(type(str(1)))
print(random.random())
print(os.path.dirname(__file__))
print (os.path.abspath(os.path.join(os.getcwd())))

msg="1"

md5=hashlib.md5(msg.encode('utf8')).hexdigest()
print(md5)

sha1=hashlib.sha1(msg.encode('utf8')).hexdigest()
print(sha1)

sha256=hashlib.sha256(msg.encode('utf8')).hexdigest()
print(sha256)
'''